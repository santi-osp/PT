# DataGo frontend

Angular 21 standalone application for residential-customer operations.

```powershell
npm install
npm start       # http://localhost:4200
npm run build   # production bundle in dist/frontend
```

Run the ASP.NET Core API at `http://localhost:5124` in a second terminal. Development and production API bases are centralized under `src/environments/`.
